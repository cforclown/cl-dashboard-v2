import React from 'react';

import { Card, CardHeader, CardBody } from '../../content/card';

import './index.scss';

function Issues() {
  return (
    <Card>
      <CardHeader>
        <h6>ISSUES</h6>
      </CardHeader>
      <CardBody />
    </Card>
  );
}

export default Issues;
